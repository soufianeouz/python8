[tool.poetry]
name = "loading"
version = "0.1.0"
description = ""
authors = ["selouzig"]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "*"
pandas = "*"
matplotlib = "*"
requests = "*"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"